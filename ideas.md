# デザイン検討：Nuxt 画面表示デバッグ演習

## 3つの方向性

### 1. Signal Trace Notebook

- **Very Brief Intro**: 障害報告、観測値、仮説、修正を調査ノートの見開きとして並べる。実務の切り分け手順を落ち着いた紙面で追体験できる方向性。
- **Probability**: 0.07

### 2. Observatory Console

- **Very Brief Intro**: ブラウザ画面、SSR出力、クライアント状態を観測パネルとして対比する。表示差異を「見えない状態のズレ」として扱う学習環境。
- **Probability**: 0.04

### 3. Incident Evidence Board

- **Very Brief Intro**: 付箋、証拠カード、時系列の糸を使い、原因を絞る過程を捜査ボードのように表現する。手掛かりの優先順位を体感的に理解できる方向性。
- **Probability**: 0.09

---

## 選定方針：Observatory Console

### Design Movement

1960年代の科学観測機器と現代のデベロッパーツールを組み合わせた **Scientific Instrument UI**。画面に現れる結果と、その背後にあるSSR・クライアント状態を計測値として対比する。

### Core Principles

1. **観測可能性を優先する**：推測ではなく、画面・HTML・ログ・状態の順に証拠を提示する。
2. **失敗と修正を同じ強さで可視化する**：失敗側を隠さず、修正後との差分を主役にする。
3. **時系列を崩さない**：報告、再現、観測、仮説、修正、再発防止を読み順そのものとして設計する。
4. **技術用語に着地を与える**：各用語の直後に、この演習で何が起きたかを示す。

### Color Philosophy

基調は温かみのある紙白とグラファイト。異常は朱色、確定した観測値は深い青緑、修正済みは苔色で表す。冷たい監視画面ではなく、慎重に事実を集める研究ノートの信頼感をつくる。

### Layout Paradigm

左に「現在の調査地点」を示す縦の工程レール、右に観測パネルを積み重ねる非対称な実験台レイアウト。中心を一枚のカードに閉じ込めず、時間と証拠が下方向へ進む構成にする。

### Signature Elements

1. SSRとClientの値を線で結ぶ二連観測窓。
2. 各工程に付く時刻風のステップ番号と状態ランプ。
3. 断定前の情報には点線、確認済みの情報には実線を使うエビデンス表現。

### Interaction Philosophy

「バグを再現」「修正を適用」の操作で状態を切り替え、画面の違いだけでなく観測ログも更新する。操作は実験装置のスイッチのように即応し、何が変わったかを局所的に強調する。

### Animation

切り替えは200ms以内のopacityとtransformに限定する。観測値は下からわずかに立ち上がり、状態ランプは一度だけ明滅する。`prefers-reduced-motion`ではすべて静止表示にする。

### Typography System

見出しには **Zen Kaku Gothic New** の700、本文には **Noto Sans JP** の400/500を採用する。コードと計測値は **IBM Plex Mono** を使い、数値の比較を一目で追えるようにする。見出しは短く断定的、本文は原因と根拠をつなぐ。

### Brand Essence

**画面で起きた事実から、NuxtのSSR表示差異を自力で解くための小さな実験室。**

性格：**検証的、静謐、実務的**。

### Brand Voice

見出し・CTA・補足は、安易に原因を断定せず観測の順序を示す。

> 「画面がずれた。まず、サーバーが何を描いたかを見る。」

> 「修正を適用し、同じ条件でも値が一致するか確認する。」

### Wordmark & Logo

複数の観測窓が中央で揃う形をした、角の丸い二重角括弧のシンボル。文字を使わず、SSRとClientの二つの出力が一致する様子を抽象化する。

### Signature Brand Color

**観測青緑 `#0F766E`**。確定した事実と一致した状態を示す、プロジェクト固有の信号色とする。

## Style Decisions

- 主要な画面は白地・濃色文字を維持し、異常状態は朱色、修正済み状態は苔色で明確に分離する。
- UIは中央寄せの紹介ページではなく、左工程レールと右観測領域の非対称レイアウトにする。
- 図解は装飾ではなく、SSR値とクライアント値の差異を説明するためにのみ使う。
- ヘッダーの記号は、中央へ揃う二重の観測窓と水平軸からなるブランド固有の計測マークとする。
- SSRとClientの値は必ず対の観測窓で提示し、不一致は朱色の点線、初期値の一致は苔色の実線で接続する。
- `#0F766E`は確定した観測と主計測ラベル、朱色と苔色は異常・修正の状態証拠だけに使う。
